# STRENTOR Prelaunch Checklist

Date: 2026-07-26

Status legend: ✅ done and verified in this engagement · ⚠️ partially addressed, needs follow-up · ❌ not started, requires business/legal/manual action outside what code can do.

| Area | Status | Notes |
|---|---|---|
| **Copy approval** | ❌ | All new/rewritten copy (homepage, Apply for Access, Programs, Transformation Stories, The STRENTOR Method, FAQ, Corporate CTA) needs a founder/business read-through before launch. Nothing was fabricated, but tone and phrasing are AI-drafted and deserve a human pass. |
| **Credential verification** | ✅ | STRENTOR confirmed founder credentials (certified fitness trainer, para powerlifter, Spina Bifida/CKD lived experience) are verified. See `STRENTOR_Website_Content_Verification.md` item 7. |
| **Testimonial permission** | ✅ | STRENTOR confirmed all existing testimonials were submitted by the clients themselves. See `STRENTOR_Website_Content_Verification.md` item 4. New public testimonial submission (`/transformation-stories/share`) publishes immediately with no review gate and no admin unpublish UI yet — a standing, explicitly-accepted risk, not a permission gap. See item 9 in the same doc. |
| **Media-logo permission** | N/A | No media/press logos were found anywhere in the current codebase to review. If any exist outside the repo, they weren't part of this audit. |
| **Pricing verification** | ✅ | STRENTOR confirmed the PPP-tiered per-session model (`utils/pricing/sessionPricing.ts`) is current and authoritative for direct coaching. The old flat ₹74,999 "program true value" (`config/accessTiers.ts`) was dead code and deleted. `config/sponsorshipOptions.ts`/`config/partnerPricing.ts` (donor and corporate pricing) confirmed as intentional flat figures, not meant to track the per-session engine — no change needed. See `STRENTOR_Website_Content_Verification.md` item 3 and item 8. |
| **Form destination** | ⚠️ | `/apply-for-access`'s `IntakeForm` posts to `/api/intake/submit`, a real, working route (not newly built in this pass). Not re-verified end-to-end (e.g. confirming the admin review queue at `/admin/intake` actually receives and displays new submissions) in this session. New: `/transformation-stories/share` posts to `/api/site-testimonials/submit`, which writes directly to the database and publishes immediately. The `site_testimonials` migration has been run against the live Supabase database (confirmed by STRENTOR), so the table now exists in production. Still not verified with an actual end-to-end browser submission (no browser available in this environment) — recommend submitting one real test testimonial through `/transformation-stories/share` and confirming it appears on `/transformation-stories` before relying on this in front of real visitors. |
| **Email notifications** | ❌ | Not verified in this pass whether form submissions trigger any notification email (via Resend, per the Privacy Policy's listed processors). Confirm this fires reliably before launch. |
| **Analytics testing** | ⚠️ | Three new custom GA4 events were implemented (`hero_cta_click`, `program_view`, `corporate_enquiry_intent` — see `STRENTOR_Analytics_Events.md`) and verified to compile and build, but not verified live in GA4's real-time report (would require a live GA property and browser session, not available in this sandboxed environment). Click through each CTA once in GA4's DebugView before relying on the data. |
| **Privacy review** | ⚠️ | A consent banner now gates Google Analytics and Meta Pixel site-wide (`components/consent/AnalyticsConsent.tsx`) — neither tracker fires until the visitor accepts. Banner wording and whether per-category (vs. single accept/reject) consent is required still need counsel. The intake form's health-data fields still sit behind one undifferentiated consent checkbox rather than a separated, explicit one — unresolved. See `STRENTOR_Privacy_Review_Required.md`. |
| **Legal-page review** | ❌ | Privacy Policy, Terms of Service, and Medical Disclaimer pages exist and were not rewritten in this pass — but Privacy Policy's listed data processors should be cross-checked against `STRENTOR_Privacy_Review_Required.md` item 3 (GA and Meta Pixel are not currently mentioned in the visible copy, as far as this review checked). |
| **Accessibility testing** | ⚠️ | No regression was introduced — all rebuilt/new pages reuse the same accessible motion primitives (`ScrollReveal`, `HoverLift`) already established as `prefers-reduced-motion`-aware elsewhere in the codebase, and one real gap (the testimonials carousel ignoring reduced motion) was found and fixed. No dedicated WCAG 2.2 AA audit (screen reader pass, full keyboard-only walkthrough, contrast check on every new copy block) was run in this session. |
| **Cross-browser testing** | ❌ | Not performed — this environment has no interactive browser session available. |
| **Mobile testing** | ❌ | Not performed interactively. New pages reuse the same responsive Tailwind patterns (container/grid breakpoints) already used elsewhere in the app, but weren't visually checked at the specific breakpoints the brief lists (320–1440px). |
| **Performance testing** | ❌ | No Lighthouse or Core Web Vitals run was performed. `npm run build` succeeded cleanly for every page touched, which confirms no build-time regression, not runtime performance. |
| **Broken-link testing** | ⚠️ | Manually checked and fixed two real broken/misdirected internal links found on `/resources` during this pass (pointing to a generic contact form instead of real pages). No automated site-wide link crawl was run. |
| **Metadata and social previews** | ⚠️ | Homepage now has its own explicit `openGraph`/`twitter` metadata (previously it would have inherited the root layout's stale, diagnosis-led fallback — also fixed). Every new page (`/transformation-stories`, `/the-strentor-method`, `/faq`) has its own `<title>`/description via a `layout.tsx`. Not visually verified in an actual social-share preview tool (e.g. pasting the URL into Slack/WhatsApp/LinkedIn) since that requires a live, publicly reachable deployment. |
| **Production environment variables** | N/A to this pass | Not touched in this engagement — see prior session history for the separate Vercel environment-variable outage that was resolved earlier. |
| **Backup and rollback procedure** | ✅ | Standard git workflow was followed throughout: every change was typechecked and built before commit, committed to the feature branch, pushed, then fast-forward merged into `main`. Any pass can be reverted independently via its own commit on `main` if needed. |

## What this checklist cannot verify from a code-only environment

Anything requiring a live browser, a real GA4/Meta Business account, an actual social-share preview, or a phone in hand was marked ❌ or ⚠️ above rather than claimed as done — this session ran in a sandboxed environment with no interactive browser or live deployment access. Treat every ❌ and ⚠️ row as a real, unfinished task, not a formality.
